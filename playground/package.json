{
  "name": "playground",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsc --watch -p tsconfig.build.json",
    "build": "rm -rf dist && vex",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@diskette/vex": "workspace:*",
    "@vanilla-extract/css": "^1.17.1",
    "@vanilla-extract/dynamic": "^2.1.5",
    "tsx": "^4.20.5"
  }
}
